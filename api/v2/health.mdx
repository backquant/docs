---
title: "Health check"
openapi: "GET /health"
description: "v2 health check (no auth). Lightweight liveness/readiness probe for external monitors."
---

Auth-free liveness probe for monitors. Returns subsystem status (Redis + Postgres).

## See also

<CardGroup cols={2}>
  <Card title="Data freshness" href="/concepts/data-freshness" icon="bookmark" />
</CardGroup>
